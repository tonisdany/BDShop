// Script para manejar el inicio de sesión document.getElementById("loginForm").addEventListener("submit", (e) => { e.preventDefault(); const role = document.getElementById("role").value; if (role === "admin") { window.location.href = "admin.html"; } else if (role === "gestora") { window.location.href = "gestora.html"; } }); // Más lógica por agregar: inventario, carrito, rankings..